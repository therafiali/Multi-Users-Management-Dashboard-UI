/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // Warning: Only disable if you are okay with potential type errors in production
    ignoreBuildErrors: true,
  },
};

export default nextConfig;